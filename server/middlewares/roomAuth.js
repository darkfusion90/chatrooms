const { getRoomByRoomId } = require('../controllers/rooms')

const ensureRoomNotPrivate = (room) => {

}

const middleware = async (req, res, next) => {
    
    switch (req.method) {
        case 'GET':
            
            break;

        default:
            break;
    }
}